This is the Repository for CPP Programming.

The link for Hello world CPP program is below;
https://ide.geeksforgeeks.org/8zjmxzhBKR

The code for Hello world program is;

#include<iostream>
int main()
{
    std::cout<<"Hello,world!!"<<std::endl;
    return 0;
}

The link for using different namespaces is below;
https://ide.geeksforgeeks.org/CD2mSZ4JXJ

The code for using different namespaces is;

#include<iostream>
namespace devops
{
    int the_solution= 42;
}

int main()
{
    std::cout<<devops::the_solution<<std::endl;
    return 0;
}

The link for defining the value returned by using datatype int is always rounded out to the number close to zero is below;
https://ide.geeksforgeeks.org/KCQE1H4TYI

The code for the same is;

#include<iostream>

int main()
{
    std::cout<<(int) 3.14<<std::endl;
    std::cout<<(int) -3.99<<std::endl;
    std::cout<<(int) 2.999<<std::endl;
    return 0;
}
.
.
.
.


#include<iostream>
using namespace std;
 
int main()
{
	cout << "Enter the length of the room: ";
	int room_length;
	cin >> room_length;

	cout << "Enter the width of the room ";
	int room_width;
	cin >> room_width;

	int area_of_the_room;
	area_of_the_room = room_length * room_width;
	cout << "The area of the room is " << area_of_the_room << " square feet\n";

	return 0;
}




#include<iostream>
using namespace std;

class cars
{

public:

	string name;
	string model;
	string price;
};

int main()
{
	cars car1;

	car1.name = "Ferrari";
	car1.model = "X500";
	car1.price = "Rs.5M";

	cout << "The name of car is:" << car1.name << endl;
	cout << "The model of the car is:" << car1.model << endl;
	cout << "The price of car is:" << car1.price << endl;

	return 0;
}

